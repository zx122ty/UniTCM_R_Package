# Get MIDAS statistics

Get MIDAS statistics

## Usage

``` r
fetch_midas_stats()
```

## Value

A named list with fields: `total_associations`, `total_genes`,
`total_diseases`, `sources`.

## Examples

``` r
if (FALSE) { # \dontrun{
fetch_midas_stats()
} # }
```
