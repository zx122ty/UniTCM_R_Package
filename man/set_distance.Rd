% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-separation.R
\name{set_distance}
\alias{set_distance}
\title{Compute closest distance between two gene sets in a network}
\usage{
set_distance(graph, setA, setB)
}
\arguments{
\item{graph}{An \code{igraph} object (the interactome / PPI network).}

\item{setA}{Character vector of gene symbols (the first node set).}

\item{setB}{Character vector of gene symbols (the second node set).}
}
\value{
A single numeric value — the mean closest distance between the
two sets. Returns \code{NaN} if no valid paths exist.
}
\description{
For each node in set B, finds the shortest-path distance to the nearest
node in set A (and vice versa), then returns the mean across all nodes.
Only nodes present in the graph are considered.
}
\examples{
\dontrun{
g <- build_ppi_network(ppi_df, all_genes)
set_distance(g, c("TP53", "BRCA1"), c("EGFR", "VEGFA", "TNF"))
}
}
