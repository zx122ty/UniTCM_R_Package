% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi-enrichment.R
\name{identify_hub_genes}
\alias{identify_hub_genes}
\title{Identify hub genes by degree centrality}
\usage{
identify_hub_genes(graph, top_n = 10L)
}
\arguments{
\item{graph}{An \code{igraph} object (from \code{\link[=build_ppi_network]{build_ppi_network()}}).}

\item{top_n}{Number of top hub genes to return. Default \code{10}.}
}
\value{
A named numeric vector of degree centrality values for the top
hub genes, sorted in descending order.
}
\description{
Calculates degree centrality for all nodes in a PPI network and returns
the top-ranked hub genes.
}
\examples{
\dontrun{
ppi <- query_string_ppi(c("TP53", "BRCA1", "EGFR", "VEGFA", "TNF"))
g <- build_ppi_network(ppi, c("TP53", "BRCA1", "EGFR", "VEGFA", "TNF"))
hubs <- identify_hub_genes(g, top_n = 5)
print(hubs)
}
}
