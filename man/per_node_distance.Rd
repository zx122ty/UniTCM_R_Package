% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-separation.R
\name{per_node_distance}
\alias{per_node_distance}
\title{Compute per-node closest distance to another gene set}
\usage{
per_node_distance(graph, setA, setB)
}
\arguments{
\item{graph}{An \code{igraph} object.}

\item{setA}{Character vector of gene symbols (query nodes).}

\item{setB}{Character vector of gene symbols (target set).}
}
\value{
A named numeric vector where names are genes from \code{setA} and
values are the closest distance to any gene in \code{setB}. Genes not
present in the network are omitted. Returns an empty named numeric
vector if no paths exist.
}
\description{
For each node in setA, finds the shortest-path distance to the nearest
node in setB. Useful for identifying which specific genes are close to
or far from a disease module.
}
\examples{
\dontrun{
g <- build_ppi_network(ppi_df, all_genes)
per_node_distance(g, c("TP53", "BRCA1"), c("EGFR", "VEGFA"))
}
}
