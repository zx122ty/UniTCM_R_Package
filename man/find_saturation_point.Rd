% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-separation.R
\name{find_saturation_point}
\alias{find_saturation_point}
\title{Find the saturation point in a curve}
\usage{
find_saturation_point(x, y, threshold = 0.01)
}
\arguments{
\item{x}{Numeric vector of x-values (e.g. N).}

\item{y}{Numeric vector of y-values (e.g. S_AB).}

\item{threshold}{Minimum absolute change required to continue adding
genes. Default \code{0.01}.}
}
\value{
An integer index into \code{x} where saturation is reached.
}
\description{
Detects where adding more genes yields less than a threshold improvement
in the metric of interest (e.g. S_AB). Uses a smoothed first difference.
}
\examples{
x <- seq(10, 100, by = 10)
y <- c(0.5, 0.3, 0.15, 0.08, 0.051, 0.049, 0.048, 0.047, 0.046, 0.046)
idx <- find_saturation_point(x, y, threshold = 0.01)
x[idx]
}
