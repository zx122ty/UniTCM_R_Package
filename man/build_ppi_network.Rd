% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi-enrichment.R
\name{build_ppi_network}
\alias{build_ppi_network}
\title{Build a PPI network as an igraph object}
\usage{
build_ppi_network(ppi_df, gene_list)
}
\arguments{
\item{ppi_df}{A data frame from \code{\link[=query_string_ppi]{query_string_ppi()}} with columns
\code{gene1}, \code{gene2}, \code{score}.}

\item{gene_list}{Character vector of all gene symbols (used to ensure
isolated nodes are included).}
}
\value{
An undirected \code{igraph} object with edge attribute \code{weight}.
}
\description{
Creates an \code{\link[igraph:graph]{igraph::graph()}} from STRING PPI edges. All genes in the
input list are included as nodes (even those without interactions, as
singletons). Edge weights are set to the STRING combined score divided
by 1000 (i.e. in [0, 1]).
}
\examples{
\dontrun{
ppi <- query_string_ppi(c("TP53", "BRCA1", "EGFR"))
g <- build_ppi_network(ppi, c("TP53", "BRCA1", "EGFR"))
igraph::vcount(g)
igraph::ecount(g)
}
}
